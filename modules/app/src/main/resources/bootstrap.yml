spring:
  application:
    name: cadastros-banco
  cloud:
    config:
      uri: http://localhost:9091

eureka:
  client:
    serviceUrl:
      defaultZone: http://${SERVICE_DISCOVERY_HOST:localhost}:8761/eureka/       